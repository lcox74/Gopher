#include <stdio.h>
#include <stdlib.h>

#include "../gopher.h"

#define CHUNK_SIZE (1024)


int main (int argc, char* argv[]) {

    /* Read gophermap to memory using `item_entry` data structure */

    /* Create a socket listening on port 70 */

    /* Thread request accepts and response with item_entry accordingly */

    return 0;
}